{% extends 'base.html.twig' %}

{% block title %}Utilisateur
{% endblock %}

{% block body %}
	<h1>Utilisateur</h1>

	<table class="table" id="roles">
		<tbody>
			<tr>
				<th>Email</th>
				<td>{{ user.email }}</td>
			</tr>
			<tr>
				<th>Rôles</th>
				<td>
					{% for role in user.userRoles %}
						{{ role.title }}
					{% endfor %}
				</td>
			</tr>
			<tr>
				<th>Mot de Passe</th>
				<td>{{ user.password }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn-sm  btn-secondary mb-1" href="{{ path('user_index') }}">Retour Liste</a>

	<a class="btn-sm  btn-secondary mb-1" href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>

	{{ include('user/_delete_form.html.twig') }}
{% endblock %}
